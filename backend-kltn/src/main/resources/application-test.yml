spring:
  config:
    activate:
      on-profile: test
  application:
    name: backend-kltn

  datasource:
    url: jdbc:mysql://localhost:3306/kltn-test?useSSL=false&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: create-drop # hoặc 'create-drop' nếu muốn tạo mới mỗi lần khởi động
    show-sql: true
  data:
    redis:
      host: ${SPRING_REDIS_HOST:localhost}
      port: ${SPRING_REDIS_PORT:6379}
jwt:
  secret: k8wT5KF1ZrFojvpXJbhXOcYVv44lCKvOwQudCRY6HjxxTGH3WQbQDHEFZjhS+mCE

  header: Authorization
  prefix: Bearer
  expirationMs: 86400000 # 1 ngày tính bằng milliseconds
server:
  port: 8081